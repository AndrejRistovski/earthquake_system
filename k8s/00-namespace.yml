# Dedicated namespace so every earthquake object is isolated from `default`
# and can be torn down / inspected as a unit (rubric item 5).
apiVersion: v1
kind: Namespace
metadata:
  name: earthquake
  labels:
    app.kubernetes.io/part-of: earthquake-system
